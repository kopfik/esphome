## components/buses/spi.yaml
#
# Inline:
#   spi_bus: !include { file: components/buses/spi.yaml, vars: { id: spi_bus, clk_pin: GPIO3, mosi_pin: GPIO8 } }
#
# Block:
#   spi_bus: !include
#     file: components/buses/spi.yaml
#     vars:
#       id: spi_bus
#       clk_pin: GPIO3
#       mosi_pin: GPIO8

spi:
  id: ${id}
  clk_pin: ${clk_pin}
  mosi_pin: ${mosi_pin}
